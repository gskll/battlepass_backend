// This file is automatically generated by Keystone, do not modify it manually.
// Modify your Keystone config when you want to change this.

datasource sqlite {
  url      = env("DATABASE_URL")
  provider = "sqlite"
}

generator client {
  provider = "prisma-client-js"
  output   = "node_modules/.prisma/client"
}

model BattlePass {
  id          String   @id @default(cuid())
  name        String   @default("")
  description String   @default("")
  status      String?  @default("DRAFT")
  start_date  DateTime
  end_date    DateTime
  experience  Int
  tiers       Tier[]   @relation("Tier_battlepass")
}

model Tier {
  id           String      @id @default(cuid())
  name         String      @default("")
  price        Int?
  battlepass   BattlePass? @relation("Tier_battlepass", fields: [battlepassId], references: [id])
  battlepassId String?     @map("battlepass")
  levels       Level[]     @relation("Level_tier")

  @@index([battlepassId])
}

model Level {
  id     String   @id @default(cuid())
  name   String   @default("")
  tier   Tier?    @relation("Level_tier", fields: [tierId], references: [id])
  tierId String?  @map("tier")
  reward Reward[] @relation("Reward_level")

  @@index([tierId])
}

model Reward {
  id          String  @id @default(cuid())
  name        String  @default("")
  description String  @default("")
  type        String?
  rarity      String?
  level       Level?  @relation("Reward_level", fields: [levelId], references: [id])
  levelId     String? @map("level")

  @@index([levelId])
}