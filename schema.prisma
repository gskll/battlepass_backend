// This file is automatically generated by Keystone, do not modify it manually.
// Modify your Keystone config when you want to change this.

datasource sqlite {
  url      = env("DATABASE_URL")
  provider = "sqlite"
}

generator client {
  provider = "prisma-client-js"
  output   = "node_modules/.prisma/client"
}

model BattlePass {
  id          String    @id @default(cuid())
  name        String    @default("")
  description String    @default("")
  status      String?   @default("DRAFT")
  start_date  DateTime
  end_date    DateTime
  experience  Int
  tiers       Tier[]    @relation("Tier_battlepass")
  missions    Mission[] @relation("Mission_battlepass")
  rewards     Reward[]  @relation("Reward_battlepass")
}

model Tier {
  id           String      @id @default(cuid())
  name         String      @default("")
  price        Int?
  battlepass   BattlePass? @relation("Tier_battlepass", fields: [battlepassId], references: [id])
  battlepassId String?     @map("battlepass")
  levels       Level[]     @relation("Level_tier")
  rewards      Reward[]    @relation("Reward_tier")

  @@index([battlepassId])
}

model Level {
  id       String  @id @default(cuid())
  name     String  @default("")
  tier     Tier?   @relation("Level_tier", fields: [tierId], references: [id])
  tierId   String? @map("tier")
  reward   Reward? @relation("Level_reward", fields: [rewardId], references: [id])
  rewardId String? @unique @map("reward")

  @@index([tierId])
}

model Reward {
  id           String      @id @default(cuid())
  name         String      @default("")
  description  String      @default("")
  type         String?
  rarity       String?
  level        Level?      @relation("Level_reward")
  tier         Tier?       @relation("Reward_tier", fields: [tierId], references: [id])
  tierId       String?     @map("tier")
  battlepass   BattlePass? @relation("Reward_battlepass", fields: [battlepassId], references: [id])
  battlepassId String?     @map("battlepass")

  @@index([tierId])
  @@index([battlepassId])
}

model Mission {
  id           String      @id @default(cuid())
  name         String      @default("")
  description  String      @default("")
  type         String?
  goal_type    String?
  battlepass   BattlePass? @relation("Mission_battlepass", fields: [battlepassId], references: [id])
  battlepassId String?     @map("battlepass")

  @@index([battlepassId])
}